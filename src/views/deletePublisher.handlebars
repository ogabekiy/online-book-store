<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Publisherni o'chirish</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #74ebd5, #ACB6E5); /* Gradient fon */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
            width: 350px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .container:hover {
            transform: translateY(-10px); /* Hover effekti */
        }

        h1 {
            color: #333;
            font-size: 30px;
            margin-bottom: 20px;
        }

        form p {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-size: 14px;
            color: #555;
        }

        input[type="number"] {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 10px;
            font-size: 16px;
            background-color: #f9f9f9;
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.05);
            transition: border 0.3s ease;
        }

        input[type="number"]:focus {
            border-color: #74ebd5;
            outline: none;
        }

        button {
            width: 100%;
            padding: 12px 15px;
            background: linear-gradient(to right, #a855f7, #d946ef);
            border: none;
            border-radius: 10px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        button:hover {
            background-color: #9a34d1;
            transform: translateY(-5px); /* Hover effekti tugmada */
        }

        /* Boshqa sahifalarga havola */
        .nav-link {
            margin-top: 20px;
            font-size: 14px;
            color: #666;
        }

        .nav-link a {
            color: #a855f7;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .nav-link a:hover {
            color: #d946ef;
        }

        /* Responsivlik uchun media query */
        @media (max-width: 400px) {
            .container {
                width: 100%;
                padding: 20px;
                margin: 0 20px;
            }
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Publisherni o'chirish</h1>
        <form id="deleteForm">
            <p>
                <label for="id">O'chirmoqchi bo'lgan publisher ID sini kiriting:</label>
                <input type="number" name="id" id="id" required placeholder="Publisher ID sini kiriting" />
            </p>
            <button type="submit">Jo'natish</button>
        </form>

        <div class="nav-link">
            <p><a href="/publisher/all">Barcha publisherlarni ko'rish</a></p>
        </div>
    </div>

    <script>
        const form = document.getElementById("deleteForm");
        form.addEventListener("submit", async (e) => {
            e.preventDefault();
            const id = document.getElementById("id").value;
            const response = await fetch(`/publisher/delete/${id}`, { method: "DELETE" });
            if (response.ok) {
                alert("Publisher muvaffaqiyatli o'chirildi");
            } else {
                alert("Xato yuz berdi");
            }
        });
    </script>
</body>
</html>
